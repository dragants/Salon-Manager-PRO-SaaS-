{
  "name": "salon-manager-pro",
  "private": true,
  "description": "Jedan terminal: backend + frontend",
  "scripts": {
    "dev": "concurrently -n api,web -c cyan,magenta \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:stack": "docker compose up -d && npm run dev",
    "start": "concurrently -n api,web \"npm start --prefix backend\" \"npm start --prefix frontend\"",
    "lan:firewall": "powershell -ExecutionPolicy Bypass -NoProfile -File scripts/windows-allow-salon-lan.ps1",
    "desktop": "npm start --prefix desktop"
  },
  "devDependencies": {
    "concurrently": "^9.2.0"
  },
  "dependencies": {
    "@dnd-kit/core": "^6.3.1",
    "@dnd-kit/sortable": "^10.0.0"
  }
}
